apiVersion: 1

# Root policy: all alerts go to Default contact point. Refine in UI: Alerting → Notification policies.
policies:
  - orgId: 1
    receiver: Default
    group_by: ['alertname', 'grafana_folder']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
