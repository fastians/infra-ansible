---
# Set to true (e.g. -e skip_apt_upgrade=true) to skip apt upgrade.
skip_apt_upgrade: true
# Skip apt upgrade when root free space is below this (bytes). 512MB default.
apt_upgrade_min_free_bytes: 536870912
