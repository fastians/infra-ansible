[Unit]
Description=Loki
After=network.target

[Service]
User=loki
WorkingDirectory=/var/lib/loki
ExecStart={{ loki_binary_path }} -config.file=/etc/loki/config.yml
Restart=always
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
