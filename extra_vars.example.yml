# Copy to extra_vars.yml and fill in. Use: ansible-playbook ... -e @extra_vars.yml
# Add extra_vars.yml to .gitignore so secrets are not committed.

skip_apt_upgrade: true

alertmanager_telegram_bot_token: "YOUR_BOT_TOKEN"
alertmanager_telegram_chat_id: "YOUR_CHAT_ID"
